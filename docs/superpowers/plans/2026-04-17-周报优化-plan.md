# 周报优化 - 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 对 H5 周报系统进行优化：章节重命名、流程简化、UI 简化、导出简化

**Architecture:**
- 章节名称变更：修改 types 常量和 AI prompt
- 流程简化：HomePage.vue 删除归类预览步骤，直接跳转到模板选择
- 编辑页改造：用 CodeMirror 替换富文本编辑器，删除保存功能
- 导出简化：删除 Word/PDF 导出，仅保留 MD

**Tech Stack:** Vue 3, TypeScript, CodeMirror 6

---

## Phase 1: 章节重命名

### Task 1: 更新类型定义中的章节名称

**Files:**
- Modify: `src/types/classification.ts`
- Modify: `src/types/report.ts`

- [ ] **Step 1: 更新 classification.ts 中的章节名称常量**

修改 `PRODUCT_GROUP_ORDER` 和相关常量：
```typescript
// 原来
'来福商城',
'三方项目',

// 改为
'来福商城与卡册',
'三方对接项目',
```

修改 `PRODUCT_GROUP_COLORS`：
```typescript
// 原来
'来福商城': '#1890ff',
'三方项目': '#52c41a',

// 改为
'来福商城与卡册': '#1890ff',
'三方对接项目': '#52c41a',
```

- [ ] **Step 2: 更新 report.ts 中的 ChapterId**

```typescript
// 原来
LAIFU: 'chapter-1' as ChapterId,  // 来福商城
THIRD_PARTY: 'chapter-2' as ChapterId,  // 三方项目 (开发中)

// 改为
LAIFU: 'chapter-1' as ChapterId,  // 来福商城与卡册
THIRD_PARTY: 'chapter-2' as ChapterId,  // 三方对接项目
```

- [ ] **Step 3: 更新 useTemplateManager.ts 中的章节名称**

- [ ] **Step 4: 验证 TypeScript 编译**

```bash
npm run build 2>&1 | tail -10
```

---

### Task 2: 更新 AI 归类 prompt

**Files:**
- Modify: `src/composables/useLLMParser.ts`
- Modify: `src/composables/useClassificationStore.ts`

- [ ] **Step 1: 更新 useLLMParser.ts 中的 CHAPTER_META**

```typescript
// 原来
{ id: 'chapter-1', title: '来福商城', keywords: ['来福', '卡册', '如意', 'C网'], ... }
{ id: 'chapter-2', title: '三方项目 (开发中)', keywords: ['三方对接', '三方项目'], ... }

// 改为
{ id: 'chapter-1', title: '来福商城与卡册', keywords: ['来福', '卡册', '如意', 'C网'], ... }
{ id: 'chapter-2', title: '三方对接项目', keywords: ['三方对接', '三方项目'], ... }
```

- [ ] **Step 2: 更新 useClassificationStore.ts 中的 prompt**

修改 `buildClassificationPrompt` 函数中的章节描述：
```
1. 来福商城与卡册 (chapter-1) - 来福、卡册、如意、C网、会员系统、额度、转赠、促销
2. 三方对接项目 (chapter-2) - 三方对接、商城平台搭建、API对接
```

- [ ] **Step 3: 验证编译**

---

## Phase 2: 流程简化

### Task 3: HomePage 流程简化 - 删除归类预览步骤

**Files:**
- Modify: `src/pages/HomePage.vue`

- [ ] **Step 1: 找到"开始归类"按钮的处理逻辑**

在 HomePage.vue 中找到 `handlePreviewConfirm` 函数，当前逻辑是：
1. 调用 `classificationStore.classifyRows()`
2. 归类完成后进入步骤 3（归类预览）

修改为：
1. 调用 `classificationStore.classifyRows()`
2. 归类完成后**直接进入步骤 4（模板选择）**

- [ ] **Step 2: 删除 ClassificationPreview 相关组件引用**

删除模板中的 `<ClassificationPreview>` 引用（如果存在）

- [ ] **Step 3: 更新 currentStep 逻辑**

```typescript
// 原来
currentStep.value = 3  // 归类预览

// 改为
currentStep.value = 4  // 直接跳到模板选择
```

- [ ] **Step 4: 验证编译**

---

## Phase 3: 编辑页改造

### Task 4: 用 CodeMirror 替换 MarkdownEditor

**Files:**
- Create: `src/components/editor/CodeMirrorEditor.vue`
- Modify: `src/pages/EditorPage.vue`

- [ ] **Step 1: 安装 CodeMirror 依赖**

```bash
npm install @codemirror/state @codemirror/view @codemirror/lang-markdown @codemirror/theme-one-dark
```

- [ ] **Step 2: 创建 CodeMirrorEditor.vue**

创建 `src/components/editor/CodeMirrorEditor.vue`：
```vue
<template>
  <div class="codemirror-editor">
    <div ref="editorContainer" class="editor-container"></div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted, watch } from 'vue'
import { EditorState } from '@codemirror/state'
import { EditorView, keymap, lineNumbers } from '@codemirror/view'
import { markdown } from '@codemirror/lang-markdown'

const props = defineProps<{
  modelValue: string
  placeholder?: string
  disabled?: boolean
}>()

const emit = defineEmits<{
  'update:modelValue': [value: string]
}>()

const editorContainer = ref<HTMLElement>()
let editorView: EditorView | null = null

onMounted(() => {
  if (!editorContainer.value) return

  const updateListener = EditorView.updateListener.of((update) => {
    if (update.docChanged) {
      emit('update:modelValue', update.state.doc.toString())
    }
  })

  const state = EditorState.create({
    doc: props.modelValue,
    extensions: [
      markdown(),
      EditorView.lineWrapping,
      updateListener,
      EditorView.theme({
        '&': { height: '100%' },
        '.cm-scroller': { overflow: 'auto' }
      })
    ]
  })

  editorView = new EditorView({
    state,
    parent: editorContainer.value
  })
})

watch(() => props.modelValue, (newVal) => {
  if (editorView && newVal !== editorView.state.doc.toString()) {
    editorView.dispatch({
      changes: { from: 0, to: editorView.state.doc.length, insert: newVal }
    })
  }
})
</script>

<style scoped>
.codemirror-editor {
  height: 100%;
  border: 1px solid #d9d9d9;
  border-radius: 8px;
  overflow: hidden;
}

.editor-container {
  height: 100%;
}

:deep(.cm-editor) {
  height: 100%;
}

:deep(.cm-scroller) {
  font-family: 'Monaco', 'Menlo', monospace;
  font-size: 14px;
}
</style>
```

- [ ] **Step 3: 更新 EditorPage.vue**

替换 MarkdownEditor 引用：
```typescript
// 原来
import MarkdownEditor from '../components/editor/MarkdownEditor.vue'

// 改为
import CodeMirrorEditor from '../components/editor/CodeMirrorEditor.vue'
```

模板中替换：
```vue
<!-- 原来 -->
<MarkdownEditor
  v-else
  ref="markdownEditorRef"
  v-model="currentChapterContent"
  placeholder="请输入 Markdown 内容..."
  :show-preview="true"
/>

<!-- 改为 -->
<CodeMirrorEditor
  v-model="currentChapterContent"
  placeholder="请输入 Markdown 内容..."
/>
```

- [ ] **Step 4: 删除旧的 MarkdownEditor.vue**

删除 `src/components/editor/MarkdownEditor.vue`

- [ ] **Step 5: 验证编译**

---

### Task 5: 删除富文本编辑器

**Files:**
- Delete: `src/components/editor/RichTextEditor.vue`
- Modify: `src/pages/EditorPage.vue`

- [ ] **Step 1: 删除 RichTextEditor.vue**

```bash
rm src/components/editor/RichTextEditor.vue
```

- [ ] **Step 2: 更新 EditorPage.vue**

删除富文本编辑器相关代码：
- 删除 `import RichTextEditor`
- 删除 `editorMode` ref
- 删除模板中的 `<RichTextEditor>` 组件
- 删除富文本模式相关的 toolbar 按钮

- [ ] **Step 3: 验证编译**

---

### Task 6: 删除保存功能

**Files:**
- Modify: `src/pages/EditorPage.vue`

- [ ] **Step 1: 找到保存相关的函数和按钮**

删除：
- 保存按钮及相关 UI
- `saveReport` 函数（如果存在）
- localStorage 自动保存逻辑

- [ ] **Step 2: 验证编译**

---

## Phase 4: 导出简化

### Task 7: 删除 Word 和 PDF 导出

**Files:**
- Modify: `src/components/common/ExportPanel.vue`
- Modify: `src/pages/EditorPage.vue`

- [ ] **Step 1: 更新 ExportPanel.vue**

删除 Word、PDF 导出按钮，只保留 MD 导出：
```vue
<!-- 删除这些按钮 -->
<!-- <el-button @click="exportWord">导出 Word</el-button> -->
<!-- <el-button @click="exportPdf">导出 PDF</el-button> -->

<!-- 保留 -->
<el-button type="primary" @click="exportMd">导出 MD</el-button>
```

- [ ] **Step 2: 检查并删除 docx/html2pdf 依赖**

```bash
grep -r "docx\|html2pdf" src/
```

如果有相关导入，删除它们

- [ ] **Step 3: 从 package.json 删除无用依赖**

```bash
npm uninstall docx html2pdf.js
```

- [ ] **Step 4: 验证编译**

---

## Phase 5: 验证与测试

### Task 8: 最终验证

- [ ] **Step 1: TypeScript 编译检查**

```bash
npx vue-tsc --noEmit
```

- [ ] **Step 2: 构建验证**

```bash
npm run build
```

- [ ] **Step 3: 单元测试**

```bash
npm test
```

- [ ] **Step 4: E2E 测试**

```bash
npm run test:e2e
```

---

## 变更摘要

| 文件 | 变更类型 |
|------|----------|
| `src/types/classification.ts` | 修改 |
| `src/types/report.ts` | 修改 |
| `src/composables/useLLMParser.ts` | 修改 |
| `src/composables/useClassificationStore.ts` | 修改 |
| `src/composables/useTemplateManager.ts` | 修改 |
| `src/pages/HomePage.vue` | 修改 |
| `src/pages/EditorPage.vue` | 修改 |
| `src/components/editor/CodeMirrorEditor.vue` | 新建 |
| `src/components/editor/MarkdownEditor.vue` | 删除 |
| `src/components/editor/RichTextEditor.vue` | 删除 |
| `src/components/common/ExportPanel.vue` | 修改 |
| `package.json` | 修改 |

---

## 实施顺序

1. **Phase 1**: 更新章节名称（Task 1-2）
2. **Phase 2**: 流程简化（Task 3）
3. **Phase 3**: 编辑页改造（Task 4-6）
4. **Phase 4**: 导出简化（Task 7）
5. **Phase 5**: 验证测试（Task 8）
